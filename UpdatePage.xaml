<Page
    x:Class="OpenStoreUI.UpdatesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:OpenStoreUI.Models">

    <Grid RowDefinitions="Auto, *" Margin="24">
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16" Margin="0,0,0,16">
            <TextBlock Text="Available Updates" Style="{StaticResource TitleTextBlockStyle}"/>
            <Button Content="Check for Updates" Click="Refresh_Click"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <GridView ItemsSource="{x:Bind ViewModel.AvailableUpdates, Mode=OneWay}">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="models:AppPackage">
                        <Grid Width="280" Height="140" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Margin="0,0,12,12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="{x:Bind Name}" Style="{StaticResource SubtitleTextBlockStyle}" TextTrimming="CharacterEllipsis"/>
                            
                            <StackPanel Grid.Row="1" Margin="0,4,0,0">
                                <TextBlock Text="{x:Bind InstalledVersionDisplay}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind NewVersionDisplay}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            </StackPanel>

                            <Button Grid.Row="2" Content="Update" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Right" Click="Update_Click"/>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </ScrollViewer>
    </Grid>
</Page>